import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    parent: {      
      width: "98%",
      marginLeft: "1%",
      marginRight: "1%",
    }
  }));
  // '& > *': {
  //   width: '100%',
  // }
